[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py", "*_test.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
addopts = [
    "-v",
    "--tb=short"
]

# Coverage options (only used when pytest-cov is installed)
# addopts = [
#     "-v",
#     "--tb=short",
#     "--cov=src",
#     "--cov-report=term-missing",
#     "--cov-report=xml",
#     "--cov-fail-under=80"
# ]